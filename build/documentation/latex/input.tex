SOMA offers a designated input format. The purpose of this format is to be human readable in contrast to the hdf5 input files, which are optimized for the computation, both IO wise and optimal for computational purposes. For this reason the hdf5 file format might change at any time, if the performance can be increased with these changes. On the other hand to offer stability for the user the helper scripts and the XML format should change as less as possible.~\newline
 On this page we describe the input format, so that any user can create its own input file for her/his purpose.\hypertarget{input_example}{}\doxysection{Example Input File}\label{input_example}
The input format is based on \href{https://en.wikipedia.org/wiki/XML}{\texttt{ xml}} and for the description we use an ASCII representation of Molecules that is inspired by the \href{http://jcheminf.springeropen.com/articles/10.1186/1758-2946-3-1}{\texttt{ Curley-\/\+Smiles}} standard.

\begin{DoxyVerb}\note We do not cover the full Curley-Smiles spectrum and we have our own flavor.

An example input for a small system of linear diblock-copolymers may look like:

\include coord.xml

But let us discuss the tags step by step. In general the order of
the tags is without meaning, while the hierachy is not. There are
a few execeptions, where is order has a meaning. If there are more
than one tag of the same type on the same hierachy level, usually
their context will be summarized. In addition, in the "text" region of xml tags usually blank or lines beginning with '#' are ignored.

- \code <xml>\endcode introduces the file as xml
- \code <soma> \endcode Specifies that this xml is a SOMA input file. This tag is the root for all other tags. Anything outside will be ignored.
- \code <interactions>\endcode Is defining the interaction of the particle of different types. There are 3 sub-tags expeceted:
  -\code <kappaN>\endcode Defines \f$ \kappa N\f$ the interaction between particles of the same type.
  -\code <chiN> \endcode Defines \f$ \chi N\f$, the interaction
   between 2 different particle types. The syntax is line based
   and expects "typeA typeB value" for each unequal type. If you
   specify \f$ \chi N \f$ for equal or \a equivalent particle
   types the parsing will fail.
   \note \f$ N \f$ of \f$ \chi N \f$ and \f$ \kappa N\f$ refers to the reference N.
 - \code <bonds>\endcode Specifies the bond type between any half-bond type. The syntax is line based and expectes "typeA typeB bond-type".
- \code <A> \endcode Defines the mobility of the different particle
  types in the smart Monte-Carlo moves. The syntax is line based and
  expects "type value", where value is the friction of the Langevin
  Dynamics.
  \note The value corresponds to "dt*N/xi" of the old format divided by "reference_Nbeads"

  Alternatively, you specify after your text based input an \code <dt> \endcode tag.
  This value is exactly identical to the input "dt*N/xi" of the old input format.
  If this tag is specified the default "A" for every type is calculated. You may omit
  any text input if you like the default value. But with the text you can overwrite the
  mobility for any type.
  \warning The order of text based input and "dt" tag is important. First specify the text based input.
  After the text you can add a "dt" tag.
  \note You may specify a mobility of zero for a type, to get an immobile anchor.
  Which can be desired for the end monomer of a brush.
- \code <time> \endcode Defines the timestep of the configuration.
- \code <hamiltonian> \endcode Defines the non bonded hamiltonian of the configuration.
If not specified the default "SCMF0" is selected. For options refer "enum Hamiltonian" in struct.h.
- \code <reference_Nbeads> \endcode Defines the number of
  reference beads of the configuration. All values that depend on
  a number of monomers refer to this value. This is required
  because SOMA supports the simulation of multiple chain
  architectures with varying number of monomers.

  \note Even the length unit depends on this value, because the
   length unit is in units of \f$ Re \f$, which is defined by a number of
    beads.
- \code <lxyz> \endcode X,Y,Z of the box dimensions in units of \f$ Re \f$.
- \code <nxyz> \endcode X,Y,Z of the grid dimensions in grid units.
- \code <poly_arch> \endcode Curley-Smiles description of a simple linear diblock-copolymer.
  For more details refer to \ref curley-smiles
- \code <analysis> \endcode Description of Analysis elements.
  - SOMA uses a separate analysis file to observable states of the simulation on the fly.
  - To describe, which observable is analysed how, this section is used.
  - Each Oberservable has its own tag and all modifier go there.
  - Inside of an observable tag, each new tag is interpreted as an individual hdf5 attribute for the element.
    - The tag is the Name of the attribute and the content is converted via numpy.loadtxt to a matrix of integer as attr data.
  - \code DeltaMC \endcode Each obs has an DeltaMC field. This specifies how often in MC steps this obs is analysed.
  - \code end_mono \endcode Is a special attribute of the "Re" observable. Specifying which monomers of a polytype are considered as "end-monomers".
    Matrix shape, should be n_poly_types x 2. The first dimension is counting the poly_type the second start and end monomer. Refer the complex example for an example.
  - \code <dynamical_structure_factor> (<static_structure_factor>) \endcode measures the dynamical structure factor (static structure factor) of each type of polymer and save if to ana file.
    - \code <DeltaMC> \endcode defines the frequency where the RCMs are checked and the rules applied.
    - \code <q> \endcode defines the norm of the scattering vector. With given norm q, the dynamical structure factor will be calculated with randomly generated q_x,q_y,q_z(three components of the scattering vector) uniformly distributed in spherical coordinate. This measurement will be repeated N times and the result is the average of the N values. The number of random scattering vector N is set to 32 as default and can be changed as command line argument when starting SOMA.


\subsection confgen.py The Conversion Script.
This example should give you a rough overview over the input
format. Such a file can be converted by the "./ConfGen.py" script.
\code ./ConfGen.py -i coord.xml -o coord.h5 \endcode
The resulting hdf5 file can be used as an input hdf5 for the SOMA tool.

It will not generate the position for all beads, but SOMA detects
the lack of beads and automatically generates this information at
the startup.
\warning Be careful if you set up configuration containing rings. The random scheme recursively generate

The script can also be used to change parameter of an existing hdf5 ("old.hdf5") configuration. The procedure is as follows:
- Prepare a xml file containing all information of your existing configuration.
\warning You cannot leave out any data field.
- Modify this file according to your needs. ("mod.xml")
- Run the script:
\code ./ConfGen.py -i mod.xml -o old.hdf5 --udpate\endcode

The script compares the parameter of the xml file with hdf5 file
and updates any differences. The script prints out messages about
changed fields.<br> You can change almost any parameter except the
number of total polymers, the number of polymer architectures and
the number of monomers for each polymer. An AssertionError will be
raised if you try such a modification and the original hdf5 is
kept unchanged.
\end{DoxyVerb}
 \hypertarget{input_curley-smiles}{}\doxysection{SOMA Flavor of Curley Smiles}\label{input_curley-smiles}
To enable you to describe you molecule for SOMA in a human-\/ and machine-\/readable way SOMA uses its own flavor of the Curley-\/\+Smiles standard to describe your molecule architecture with characters.~\newline


A molecule is described by a single line. The first part of the line specifies the number of molecules of this type. This is followed by a Curley-\/\+Smiles string to describe the molecule architecture.~\newline


In general the basis of each molecule is here a main backbone of a linear molecule. In addition, you can specify branches of this backbone (and inside branches) and additional bonds on branch level. This enables almost any molecule architecture. (But not all architectures.)

To achieve this there are two basic elements of which a every molecule description is composed.
\begin{DoxyItemize}
\item A molecule part. This specifies a linear piece of a molecule. And defines additional connections. Such a molecular part has 3 region and no whitespaces.
\begin{DoxyItemize}
\item The first region is the name of the monomer type. The is a string enclosed by \char`\"{}\textquotesingle{}\char`\"{}. Inside you can use any number of whitespaces.
\item The second region is a list of additional bond tags. These tags are integer number and are separated by the \char`\"{}\%\char`\"{} character. The scope of this tags is the branch, so you can overshadow an index in following branches. In this branch additional bond between monomers with the same tag are introduced. The \char`\"{}0\char`\"{} tag is special. Any monomer has to have this tag, but it introduces no additional bond.
\item The last region is the \char`\"{}\+Curley\char`\"{} part. In curly brackets \char`\"{}\{\}\char`\"{} enclosed is an integer number which specifies the repetition of this linear segment.
\end{DoxyItemize}
\item A branch is enclosed by normal brackets \char`\"{}()\char`\"{} and encloses any number of molecule parts and branches. A closing branch \char`\"{})\char`\"{} is followed by a curly repetition region, which specify how many of the branches are used. After a branch another branch or a molecule part follows.
\end{DoxyItemize}

To make the life of you easier you can apply a number of abbreviation to your Curly-\/\+Smiles string.
\begin{DoxyItemize}
\item The tag \char`\"{}0\char`\"{} of every monomer can be left out.
\item The \char`\"{}\%\char`\"{} at the end of a tag list is optional
\item If your type name is a single \char`\"{}alpha\char`\"{}-\/character you can omit the enclosing \char`\"{}\textquotesingle{}\char`\"{}.
\item If you want a repetition of 1, you can omit the \char`\"{}\{1\}\char`\"{}.
\end{DoxyItemize}\hypertarget{input_example-mol}{}\doxysubsection{Example Molecules}\label{input_example-mol}
Here are a few examples of increasing complexity.
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{100 A\{16\}B\{16\}}

\end{DoxyCode}
 are 100 diblock copolymers with 16 A monomers followed by 16 B monomers.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{100 A\{16\}(B\{16\})C\{16\}}

\end{DoxyCode}
 are 100 star polymers with 3 arms of 16 monomers each. The type of the branches is A,B,C respectively. An equivalent description is 
\begin{DoxyCode}{0}
\DoxyCodeLine{100 A\{16\}(B\{16\})(C\{16\})}

\end{DoxyCode}

\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{100 A1A\{28\}A1 }

\end{DoxyCode}
 are 100 rings of 30 A monomers.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{100 A\{16\}(B\{4\})\{10\} }

\end{DoxyCode}
 are 100 linear A strands of 16 monomers, which branch into 10 equal branches of 4 B monomers.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{1 A1A2A1\{2\}(\textcolor{charliteral}{'B'}\{2\}A1(\textcolor{stringliteral}{'Qe'}\{2\})\{2\}AB1)\{2\}A1\%2}

\end{DoxyCode}
 A drawing of this input molecule looks like\+: 
\begin{DoxyImageNoCaption}
  \mbox{\includegraphics[width=\textwidth,height=\textheight/2,keepaspectratio=true]{dot_inline_dotgraph_1}}
\end{DoxyImageNoCaption}
 \begin{DoxyNote}{Note}
If you like a graphical representation of your molecule, run 
\begin{DoxyCode}{0}
\DoxyCodeLine{./ConfGen.py -\/-\/dot-\/graph }

\end{DoxyCode}
 with the corresponding flag. You will get for each molecule a \char`\"{}dot\char`\"{} representation of your molecule. Using the \char`\"{}dot\char`\"{} tool from the Graphviz package you can convert this output to visual graphs.
\end{DoxyNote}

\end{DoxyItemize}\hypertarget{input_complex}{}\doxysubsection{A More Complex Example}\label{input_complex}
Here we show a more complex input file, which uses a few more options and tags. 
\begin{DoxyCodeInclude}{0}
\DoxyCodeLine{<?\textcolor{keyword}{xml} \textcolor{keyword}{version}=\textcolor{stringliteral}{"{}1.0"{}} \textcolor{keyword}{encoding}=\textcolor{stringliteral}{"{}UTF-\/8"{}} \textcolor{keyword}{standalone}=\textcolor{stringliteral}{"{}yes"{}}?>}
\DoxyCodeLine{<\textcolor{keywordtype}{soma} \textcolor{keyword}{version}=\textcolor{stringliteral}{"{}0.2.0"{}}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{interactions}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{kappaN}>}
\DoxyCodeLine{      30}
\DoxyCodeLine{    </\textcolor{keywordtype}{kappaN}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{chiN}>}
\DoxyCodeLine{      \textcolor{keyword}{A} \textcolor{keyword}{B} 20}
\DoxyCodeLine{    </\textcolor{keywordtype}{chiN}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{bonds}>}
\DoxyCodeLine{    \textcolor{keyword}{A} \textcolor{keyword}{A} \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    \textcolor{keyword}{A} \textcolor{keyword}{B} \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    \textcolor{keyword}{A} \textcolor{stringliteral}{'Qe'} \textcolor{keyword}{HARMONICVARIABLESCALE}}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{keyword}{B} \textcolor{keyword}{B} \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    \textcolor{keyword}{B} \textcolor{stringliteral}{'Qe'} \textcolor{keyword}{harmonic}}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{stringliteral}{'Qe'} \textcolor{stringliteral}{'Qe'} \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    </\textcolor{keywordtype}{bonds}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{interactions}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{equivalent\_particle\_types}>}
\DoxyCodeLine{    \textcolor{keyword}{A} \textcolor{stringliteral}{'Qe'}}
\DoxyCodeLine{  </\textcolor{keywordtype}{equivalent\_particle\_types}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{A}>}
\DoxyCodeLine{    \# \textcolor{keyword}{mobility} \textcolor{keyword}{of} \textcolor{keyword}{A} \textcolor{keyword}{is} \textcolor{keyword}{overwritten}}
\DoxyCodeLine{    \textcolor{keyword}{A} 0.003}
\DoxyCodeLine{}
\DoxyCodeLine{    \# \textcolor{keyword}{B} \textcolor{keyword}{uses} \textcolor{keyword}{the} \textcolor{keyword}{default} \textcolor{keyword}{mobility} \textcolor{keyword}{of} \textcolor{keyword}{dt},}
\DoxyCodeLine{    \# \textcolor{keyword}{because} \textcolor{keyword}{it} \textcolor{keyword}{is} \textcolor{keyword}{not} \textcolor{keyword}{mentioned} \textcolor{keyword}{in} \textcolor{keyword}{the} \textcolor{keyword}{list.}}
\DoxyCodeLine{    <\textcolor{keywordtype}{dt}>}
\DoxyCodeLine{      0.17}
\DoxyCodeLine{    </\textcolor{keywordtype}{dt}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{A}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{time}>}
\DoxyCodeLine{    42}
\DoxyCodeLine{  </\textcolor{keywordtype}{time}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{harmonicvariablescale}>}
\DoxyCodeLine{    1.25}
\DoxyCodeLine{  </\textcolor{keywordtype}{harmonicvariablescale}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{poly\_arch}>}
\DoxyCodeLine{    200 \textcolor{keyword}{A1A2A1}\{2\}(\textcolor{stringliteral}{'B'}\{2\}\textcolor{keyword}{A1}(\textcolor{stringliteral}{'Qe'}\{2\})\{2\}\textcolor{keyword}{AB1})\{2\}\textcolor{keyword}{A1}\%2}
\DoxyCodeLine{    11 \textcolor{keyword}{A}\{64\}\textcolor{keyword}{B}\{64\}}
\DoxyCodeLine{    11 \textcolor{keyword}{B}\{64\}\textcolor{keyword}{A}\{64\}}
\DoxyCodeLine{  </\textcolor{keywordtype}{poly\_arch}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{reference\_Nbeads}>}
\DoxyCodeLine{    64}
\DoxyCodeLine{  </\textcolor{keywordtype}{reference\_Nbeads}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{poly\_arch}>}
\DoxyCodeLine{    100 \textcolor{keyword}{A}\{5\}(\textcolor{stringliteral}{'Qe'}\{5\}(\textcolor{keyword}{B}\{5\})\{5\})\{5\}}
\DoxyCodeLine{  </\textcolor{keywordtype}{poly\_arch}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{lxyz}>}
\DoxyCodeLine{    10.4 11.9 12.5}
\DoxyCodeLine{  </\textcolor{keywordtype}{lxyz}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{nxyz}>}
\DoxyCodeLine{    4 5 6}
\DoxyCodeLine{  </\textcolor{keywordtype}{nxyz}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{area51}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{point\_cloud}>}
\DoxyCodeLine{      1 2 3 1}
\DoxyCodeLine{    </\textcolor{keywordtype}{point\_cloud}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{box}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{point}>0 1 2</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{point}>1 2 3</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{value}>1</\textcolor{keywordtype}{value}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{box}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{cylinder}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{point}>0 0 0</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{point}>1 2 3</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{radius}>2</\textcolor{keywordtype}{radius}><\textcolor{keywordtype}{value}>1</\textcolor{keywordtype}{value}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{cylinder}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{sphere}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{point}>1 2 3</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{radius}>2</\textcolor{keywordtype}{radius}><\textcolor{keywordtype}{value}>1</\textcolor{keywordtype}{value}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{sphere}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{area51}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{external\_field}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{point\_cloud}>}
\DoxyCodeLine{      0 0 0 -\/11.5 13.42}
\DoxyCodeLine{    </\textcolor{keywordtype}{point\_cloud}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{cylinder}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{point}>0 0 0</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{point}>1 2 3</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{radius}>2</\textcolor{keywordtype}{radius}><\textcolor{keywordtype}{value}>-\/0.5 0.5</\textcolor{keywordtype}{value}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{cylinder}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{external\_field}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{polyconversion}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{      1}
\DoxyCodeLine{    </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{box}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{point}>0 1 2</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{point}>1 2 3</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{value}>0</\textcolor{keywordtype}{value}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{box}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{cylinder}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{point}>0 0 0</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{point}>1 2 3</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{radius}>2</\textcolor{keywordtype}{radius}><\textcolor{keywordtype}{value}>2</\textcolor{keywordtype}{value}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{cylinder}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{conversion\_list}>}
\DoxyCodeLine{      1 2 0}
\DoxyCodeLine{      2 1 1}
\DoxyCodeLine{      1 2 1}
\DoxyCodeLine{    </\textcolor{keywordtype}{conversion\_list}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{conversion\_rate}>}
\DoxyCodeLine{        0.002}
\DoxyCodeLine{        0.2}
\DoxyCodeLine{        0.008}
\DoxyCodeLine{    </\textcolor{keywordtype}{conversion\_rate}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{density\_dependency}>}
\DoxyCodeLine{        0 1 0}
\DoxyCodeLine{        0 0 0}
\DoxyCodeLine{        1 3 0}
\DoxyCodeLine{    </\textcolor{keywordtype}{density\_dependency}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{polyconversion}>}
\DoxyCodeLine{}
\DoxyCodeLine{  <\textcolor{keywordtype}{hamiltonian}>}
\DoxyCodeLine{    \textcolor{keyword}{SCMF1}}
\DoxyCodeLine{  </\textcolor{keywordtype}{hamiltonian}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{analysis}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{Re}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 500 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{end\_mono}>}
\DoxyCodeLine{  0 4}
\DoxyCodeLine{  0 127}
\DoxyCodeLine{  0 5}
\DoxyCodeLine{      </\textcolor{keywordtype}{end\_mono}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{Re}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{Rg}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 500 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{Rg}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{density\_var}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 500 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{density\_var}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{bond\_anisotropy}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 500 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{bond\_anisotropy}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{acc\_ratio}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 500 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{acc\_ratio}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{MSD}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 500 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{MSD}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{dump}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 50000 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{dump}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{density\_field}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 5000 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{compression}> \textcolor{keyword}{gzip} </\textcolor{keywordtype}{compression}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{density\_field}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{non\_bonded\_energy}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 5000</\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{non\_bonded\_energy}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{bonded\_energy}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 5000</\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{bonded\_energy}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{dynamical\_structure\_factor}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 50000 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{q}>0.1 1 5</\textcolor{keywordtype}{q}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{dynamical\_structure\_factor}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{static\_structure\_factor}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 50000 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{q}>0.1 1 5</\textcolor{keywordtype}{q}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{static\_structure\_factor}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{analysis}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{cm\_a}>}
\DoxyCodeLine{      0 1.0 1.0 20}
\DoxyCodeLine{    </\textcolor{keywordtype}{cm\_a}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{density\_weights}>}
\DoxyCodeLine{      \textcolor{keyword}{B} 1.5}
\DoxyCodeLine{    </\textcolor{keywordtype}{density\_weights}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{mobility\_modifier}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{type}>}
\DoxyCodeLine{  \textcolor{keyword}{MULLER\_SMITH\_MOBILITY}}
\DoxyCodeLine{      </\textcolor{keywordtype}{type}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{param}>}
\DoxyCodeLine{  1 2 3 4}
\DoxyCodeLine{      </\textcolor{keywordtype}{param}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{poly\_type\_mc\_freq}>}
\DoxyCodeLine{  4 5 6 7}
\DoxyCodeLine{      </\textcolor{keywordtype}{poly\_type\_mc\_freq}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{mobility\_modifier}>}
\DoxyCodeLine{</\textcolor{keywordtype}{soma}>}

\end{DoxyCodeInclude}
 In the following we briefly discuss the additional tags, that are used here\+:


\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<equivalent\_particle\_types>}

\end{DoxyCode}
 In case you need more types to describe your bond types than you have different interaction parameter between particles, you can specify that certain types are treated as a single particle type, but for the bond structure they are still distinguished.~\newline
 The syntax is line based and all listed monomer types are treated as equivalent.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<harmonicvariablescale> }

\end{DoxyCode}
 For bonds with type HARMONICVARIABLESCALE you can set an individual factor to scale bond length with respect to the \char`\"{}normal\char`\"{} harmonic bonds. Actually, the energy is scaled with the factor, so setting it to 2 halves the length of the HARMONICVARIABLESCALE bonds.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<cm\_a> }

\end{DoxyCode}
 Center of mass mobility of the polymer types. If this tag is specified in addition to normal Monte-\/\+Carlo moves, simple center of mass Monte-\/\+Carlo moves are applied. The data of the tag is float value for each polymer quantifying its center of mass mobility.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<area51>}

\end{DoxyCode}
 \begin{DoxyNote}{Note}
area51 applies always for all types. If after the summation on the grid a grid site has a value \texorpdfstring{$<$}{<}= 0 it is cropped to 0 and means accessible. Value bigger the 0 are cropped to 1 and mean forbidden area.
\end{DoxyNote}
\begin{DoxyWarning}{Warning}
The coordinates specified for all Grid\+Objects are in grid units, not in units of Re. That includes \texorpdfstring{$<$}{<}point\texorpdfstring{$>$}{>} tags and \texorpdfstring{$<$}{<}radius\texorpdfstring{$>$}{>} tags. Your input has to be integer, except \texorpdfstring{$<$}{<}radius\texorpdfstring{$>$}{>}.
\end{DoxyWarning}
This tag helps you to specify forbidden areas for the monomers on your grid. This can be useful if you want to simulate in confinements or you don\textquotesingle{}t want to you periodic boundary conditions. For this purpose 4 different Grid\+Object are available.
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<point\_cloud>}

\end{DoxyCode}
 The easiest object. You can specify any X,Y,Z grid site and assign it a value.~\newline
 The syntax is line base and expects first \char`\"{}x y z\char`\"{} and than a list of values. Each value applies for a type.

\begin{DoxyNote}{Note}
The values per type refer to type ids rather than types. So compare the script output with your assumption of type/type id mapping.
\end{DoxyNote}

\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<box>}

\end{DoxyCode}
 is a rectangular box. The syntax is equivalent to povray, except that SOMA uses a right-\/handed coordinate system instead of povray left-\/hand one. So with the two 
\begin{DoxyCode}{0}
\DoxyCodeLine{<point> }

\end{DoxyCode}
 tags two opposite edges are specified. The 
\begin{DoxyCode}{0}
\DoxyCodeLine{<value> }

\end{DoxyCode}
 tag captures the values for each type.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<cylinder> }

\end{DoxyCode}
 Defines a cylinder in space. The two points define the two centers of the end circles and a radius is specified. Again this is equivalent to povray.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<sphere> }

\end{DoxyCode}
 Is a sphere in space and is defined by its center and a radius.
\end{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<external\_field> }

\end{DoxyCode}
 allows to apply any external field to the grid per monomer type. The units are in $ k_B T$. The objects you can define are equivalent to the ones of \char`\"{}area51\char`\"{}. All values of a grid site are summed up.
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<time> }

\end{DoxyCode}
 allows time dependency of any external field expanded as trigonometry series.
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<period> }

\end{DoxyCode}
 is the period of the time dependent external field.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<cos> }

\end{DoxyCode}
 is the array containing the prefactor of the cosine series.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<sin> }

\end{DoxyCode}
 is the array containing the prefactor of the sine series. \begin{DoxyNote}{Note}
Objects that leave the grid are mapped back via periodic boundary conditions.
\end{DoxyNote}

\end{DoxyItemize}
\end{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<polyconversion> }

\end{DoxyCode}
 allows to define zones where the type of a polymer can be switched. If the center of mass is inside of one of the so defined zones its type is switched according to the switching rules. The definition of these rules and how the switching rules are defined is implemented via 5(3 required ones) different kinds of subtags in the XML. \begin{DoxyWarning}{Warning}
This feature can only be used with the compile option ENABLE\+\_\+\+MONOTYPE\+\_\+\+CONVERSION turned off.
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<DeltaMC> }

\end{DoxyCode}
 defines the frequency where the RCMs are checked and the rules applied. -\/
\begin{DoxyCode}{0}
\DoxyCodeLine{<cylinder> <box> <sphere> <point\_cloud> }

\end{DoxyCode}
 work exactly as with area51 or external field. They define the zone where the switching rules are applied. The value has only one entry and defines the switching rules for that reason. To be precise\+: the value indicates the starting index for the 
\begin{DoxyCode}{0}
\DoxyCodeLine{<conversion\_list> }

\end{DoxyCode}
. For example a 0 means to start reading at the beginning of that list. The user may define multiple zones with different indices. If zones overlap, the last zone overwrites the previous index value.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<conversion\_list> }

\end{DoxyCode}
 defines the reactions. There are always 3 numbers. The first defines the original polymer type the educt of the reaction. The second defines the product of the reaction. Both have to fulfill the restriction, that they enumerate valid polymer types. In addition, the number of beads between input and output must be the same. The number is read as boolean and indicates if there is another reaction. If it is false (0), the next line is also applied for the currect reaction zone cell. Otherwise no further reactions are applied.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<conversion\_rate> }

\end{DoxyCode}
 defines the reactions rates. It is always a single float per line/reaction and gives the probability to convert per conversion step. This is optional and if it is not given all polymers are converted.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<density\_dependency> }

\end{DoxyCode}
 defines on which monomer type densities the final conversion probability depends on. This is optional. For each conversion it is a list of n\+\_\+types floats. Given the density-\/dependency-\/list \char`\"{}1 0 3\char`\"{} and rate \char`\"{}0.\+001\char`\"{} the final conversion probability will be \$p = 0.\+001 \textbackslash{}cdot \textbackslash{}phi\+\_\+0 \textbackslash{}cdot \textbackslash{}phi\+\_\+2$^\wedge$3\$.
\end{DoxyItemize}
\end{DoxyWarning}

\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<density\_weights> }

\end{DoxyCode}
 Weight the beads differently as a function of their type. (Multiplication of $\phi_i$) The default weight is 1 for each type. The weights are automatically normalized such that the sum of the weights equals the number of types in the system. The normalization assures that the part of the hamiltonian that restrains the density fluctuations can compare to the total density to 1. Weights of 0 are possible, but probably not recommended. All weights zero or negative weights are obviously not an option. \begin{DoxyWarning}{Warning}
It is recommended to use the the SCMF1 hamiltonian, since the SCFM0 expects a unit density implicitly in the $ \chi N$ -\/term.
\end{DoxyWarning}

\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<mobility\_modifier> }

\end{DoxyCode}
 There are two option here and both can be used simultaneously. The first option is to vary the MC frequency as a function polymer type. 
\begin{DoxyCode}{0}
\DoxyCodeLine{<poly\_type\_mc\_freq> }

\end{DoxyCode}
 This tag expects a list of integer frequencies. 
\begin{DoxyCode}{0}
\DoxyCodeLine{time \% freq == 0 }

\end{DoxyCode}
 is the condition that an MC move is performed. The list has to have as many entries as polymer types in the configuration.

The second option is to choose different density dependent\+: Allows the modification of mobility as a function of the density surrounding the particles. This is achieved by multiplying a modification factor to the acceptance probabilty. This factor can only be in the range \mbox{[}0,1\mbox{]} and must fulfill detailed balance. Unfortunately, this can only slow particles down. At the moment two different types are implemented and to be specified with the 
\begin{DoxyCode}{0}
\DoxyCodeLine{type }

\end{DoxyCode}
 tag.
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{\mbox{\hyperlink{soma__util_8h_af9e1b45cd116d555eabb6c261ebecdbca2ae21ac2573cf346fc5e1e119855fe0b}{DEFAULT\_MOBILITY}} }

\end{DoxyCode}
 no modification (default)
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{\mbox{\hyperlink{soma__util_8h_af9e1b45cd116d555eabb6c261ebecdbca73b6a328b2c8bba36b8de4fdea8b1eed}{MULLER\_SMITH\_MOBILITY}} }

\end{DoxyCode}
 The mobility depends on the density in the current grid cell. \href{https://onlinelibrary.wiley.com/doi/abs/10.1002/polb.20385}{\texttt{ https\+://onlinelibrary.\+wiley.\+com/doi/abs/10.\+1002/polb.\+20385}} Equation 33 and 34. And Equation 20. The modifier is calculated as follows\+: $ m(r) = \frac{1}{\sum_{\alpha} a_\alpha \phi_\alpha(r) + b_\alpha \phi^2_\alpha (r)} $ $ a_\alpha $ and $ b_\alpha $ are parameters to modify the result, $ \alpha $ describes the particle type. Afterwards $ m $ is clipped to the interval $ [0,1]$. Negative values are mapped onto 1 instead of 0. These parameter are specified as a linear list $ a_A, a_B, \dots, b_A, b_B \dots $ for the required tag 
\begin{DoxyCode}{0}
\DoxyCodeLine{param }

\end{DoxyCode}
. The length of this list must be 2 $\ast$ n\+\_\+types.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{\mbox{\hyperlink{soma__util_8h_af9e1b45cd116d555eabb6c261ebecdbca51f0ad98f794c55a982f7416a2332c7e}{TANH\_MOBILITY}} }

\end{DoxyCode}
 The mobility is controlled via a sharp tanh tranistion. In contrast to MULLER\+\_\+\+SMITH\+\_\+\+MOBILITY it is possible to define one set of parameters for each type of the moving particle $i$. The equation is $ m_i(\{\phi_j\}) = 1/2 \left( 1 + \tanh\left(\frac{\phi_{0,i} - \sum_j a_{ij} \phi_j}{\Delta \phi_{i}}\right)\right) $. In total this equation requires $ 2*n_{types} + n_{types}^2 $ parameters, the order in which the parameters are expected is $\phi_{0,i}$, $\Delta \phi_i$, and $a_{i,j}$. The parameters are can be specified in flat format, or in vector $\phi_{0,i}$, vector $\Delta \phi_i$, and matrix $a_{i,j}$ format, because white spaces are ignored. It is not possible to leave out any parameter specified.
\end{DoxyItemize}
\end{DoxyItemize}\hypertarget{input_monoconversion}{}\doxysubsection{Using Monomer Type Conversions}\label{input_monoconversion}
To use monomer type conversions/ reactions, the compile option 
\begin{DoxyCode}{0}
\DoxyCodeLine{ENABLE\_MONOTYPE\_CONVERSION }

\end{DoxyCode}
 must be turned on. For this let\textquotesingle{}s look at a final simple xml-\/file that shows the use.


\begin{DoxyCodeInclude}{0}
\DoxyCodeLine{<?\textcolor{keyword}{xml} \textcolor{keyword}{version}=\textcolor{stringliteral}{"{}1.0"{}} \textcolor{keyword}{encoding}=\textcolor{stringliteral}{"{}UTF-\/8"{}} \textcolor{keyword}{standalone}=\textcolor{stringliteral}{"{}yes"{}}?>}
\DoxyCodeLine{<\textcolor{keywordtype}{soma} \textcolor{keyword}{version}=\textcolor{stringliteral}{"{}0.2.0"{}}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{interactions}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{kappaN}>}
\DoxyCodeLine{      30}
\DoxyCodeLine{    </\textcolor{keywordtype}{kappaN}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{chiN}>}
\DoxyCodeLine{      \textcolor{keyword}{A} \textcolor{keyword}{B} 20}
\DoxyCodeLine{      \textcolor{keyword}{A} \textcolor{keyword}{C} 10}
\DoxyCodeLine{      \textcolor{keyword}{B} \textcolor{keyword}{C} 0}
\DoxyCodeLine{    </\textcolor{keywordtype}{chiN}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{bonds}>}
\DoxyCodeLine{    \textcolor{keyword}{A} \textcolor{keyword}{A}  \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    \textcolor{keyword}{A} \textcolor{keyword}{B}  \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    \textcolor{keyword}{B} \textcolor{keyword}{B}  \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    \textcolor{keyword}{A} \textcolor{keyword}{C}  \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    \textcolor{keyword}{B} \textcolor{keyword}{C}  \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    \textcolor{keyword}{C} \textcolor{keyword}{C}  \textcolor{keyword}{harmonic}}
\DoxyCodeLine{    </\textcolor{keywordtype}{bonds}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{interactions}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{A}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{dt}>}
\DoxyCodeLine{    0.17}
\DoxyCodeLine{    </\textcolor{keywordtype}{dt}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{A}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{time}>}
\DoxyCodeLine{    0}
\DoxyCodeLine{  </\textcolor{keywordtype}{time}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{reference\_Nbeads}>}
\DoxyCodeLine{    64}
\DoxyCodeLine{  </\textcolor{keywordtype}{reference\_Nbeads}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{lxyz}>}
\DoxyCodeLine{    1.677 0.8385 0.8385}
\DoxyCodeLine{  </\textcolor{keywordtype}{lxyz}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{nxyz}>}
\DoxyCodeLine{    12 6 6}
\DoxyCodeLine{  </\textcolor{keywordtype}{nxyz}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{poly\_arch}>}
\DoxyCodeLine{    120 \textcolor{keyword}{A}\{32\}\textcolor{keyword}{B}\{32\}}
\DoxyCodeLine{  </\textcolor{keywordtype}{poly\_arch}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{monoconversion}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{      2}
\DoxyCodeLine{    </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{box}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{point}>0 1 2</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{point}>1 2 3</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{value}>0</\textcolor{keywordtype}{value}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{box}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{cylinder}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{point}>0 0 0</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{point}>1 2 3</\textcolor{keywordtype}{point}><\textcolor{keywordtype}{radius}>2</\textcolor{keywordtype}{radius}><\textcolor{keywordtype}{value}>2</\textcolor{keywordtype}{value}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{cylinder}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{conversion\_list}>}
\DoxyCodeLine{      \textcolor{keyword}{B} \textcolor{keyword}{A} 0}
\DoxyCodeLine{      \textcolor{keyword}{A} \textcolor{keyword}{B} 1}
\DoxyCodeLine{      \textcolor{keyword}{A} \textcolor{keyword}{C} 1}
\DoxyCodeLine{    </\textcolor{keywordtype}{conversion\_list}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{conversion\_rate}>}
\DoxyCodeLine{        0.002}
\DoxyCodeLine{        0.05}
\DoxyCodeLine{        0.008}
\DoxyCodeLine{    </\textcolor{keywordtype}{conversion\_rate}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{density\_dependency}>}
\DoxyCodeLine{        0 1 0}
\DoxyCodeLine{        0 0 0}
\DoxyCodeLine{        1 3 0}
\DoxyCodeLine{    </\textcolor{keywordtype}{density\_dependency}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{block\_size}>}
\DoxyCodeLine{        4}
\DoxyCodeLine{    </\textcolor{keywordtype}{block\_size}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{monoconversion}>}
\DoxyCodeLine{  <\textcolor{keywordtype}{analysis}>}
\DoxyCodeLine{    <\textcolor{keywordtype}{monomer\_type\_fraction}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{DeltaMC}> 2000 </\textcolor{keywordtype}{DeltaMC}>}
\DoxyCodeLine{      <\textcolor{keywordtype}{tested\_polymer}> 0 </\textcolor{keywordtype}{tested\_polymer}>}
\DoxyCodeLine{    </\textcolor{keywordtype}{monomer\_type\_fraction}>}
\DoxyCodeLine{  </\textcolor{keywordtype}{analysis}>}
\DoxyCodeLine{</\textcolor{keywordtype}{soma}>}

\end{DoxyCodeInclude}


The use is similar to the $<$polyconversion$>$-\/tag. Let\textquotesingle{}s go through the usage\+:
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<monoconversion> }

\end{DoxyCode}
 allows to define zones where the type of a monomer (or blocks of monomers) can be switched. If the bead (center of mass of a block) is inside of one of the so defined zones its type is switched according to the switching rules. The definition of these rules and how the switching rules are defined is implemented via 6(3 required ones) different kinds of subtags in the XML.
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<DeltaMC> }

\end{DoxyCode}
 defines the frequency where the RCMs are checked and the rules applied. -\/
\begin{DoxyCode}{0}
\DoxyCodeLine{<cylinder> <box> <sphere> <point\_cloud> }

\end{DoxyCode}
 work exactly as with area51 or external field. They define the zone where the switching rules are applied. The value has only one entry and defines the switching rules for that reason. To be precise\+: the value indicates the starting index for the 
\begin{DoxyCode}{0}
\DoxyCodeLine{<conversion\_list> }

\end{DoxyCode}
. For example a 0 means to start reading at the beginning of that list. The user may define multiple zones with different indices. If zones overlap, the last zone overwrites the previous index value.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<conversion\_list> }

\end{DoxyCode}
 defines the reactions. There are always 3 entries. The first defines the original monomer type, the educt of the reaction (by the name of the monomer type). The second defines the product of the reaction. Both have to fulfill the restriction, that they give a valid monomer type. The number is read as boolean and indicates if there is another reaction. If it is false (0), the next line is also applied for the currect reaction zone cell. Otherwise no further reactions are applied.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<conversion\_rate> }

\end{DoxyCode}
 defines the reactions rates. It is always a single float per line/reaction and gives the (raw) probability to convert per conversion step. This is optional and if it is not given all educt monomers are converted with probability 1.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<density\_dependency> }

\end{DoxyCode}
 defines on which monomer type densities the final conversion probability depends on. This is optional. For each conversion it is a list of n\+\_\+types floats. Given the density-\/dependency-\/list \char`\"{}1 0 3\char`\"{} and rate \char`\"{}0.\+001\char`\"{} the final conversion probability will be \$p = 0.\+001 \textbackslash{}cdot \textbackslash{}phi\+\_\+0 \textbackslash{}cdot \textbackslash{}phi\+\_\+2$^\wedge$3\$.
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{block\_size }

\end{DoxyCode}
 defines the size of monomer blocks that are converted simulatneously. In this example, the chains of length N=64 are devided into 16 blocks of size 4. It is up to the user to ensure that all polymer architectures that this might apply to are commensurate.
\end{DoxyItemize}
\end{DoxyItemize}

There is one more possible observable that can be chosen\+: The 
\begin{DoxyCode}{0}
\DoxyCodeLine{<monomer\_type\_fraction> }

\end{DoxyCode}
 allows to analyse the bead type distribution for a given polymer. The following subtags are required\+:
\begin{DoxyItemize}
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<DeltaMC> }

\end{DoxyCode}
 defines the frequency at which the polymer\textquotesingle{}s type distribution is calculated
\item 
\begin{DoxyCode}{0}
\DoxyCodeLine{<tested\_polymer> }

\end{DoxyCode}
 gives the polymer that is tested. 
\end{DoxyItemize}